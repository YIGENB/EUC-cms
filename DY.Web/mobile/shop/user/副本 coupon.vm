<div class="bor1-yellow tips mb10">
	<ul class="list-square">
		<li>优惠券仅限在订单结算时使用</li>
	</ul>
</div>
<table class="infobox order">
	<caption>
	    我所拥有的优惠券
	</caption>
	<thead>
		<tr>
			<th class="th2">号码</th>
			<th>接收人</th>
			<th>说明</th>
			<th class="th1">金额</th>
			<th class="th1">状态</th>
		</tr>
	</thead>
	<tbody>
		#foreach($row in $coupon_list)
		#set ($bonusType = $this.bonusType($row.bonus_type_id))
		<tr>
		    <td>$row.bonus_sn</td>
		    <td>$row.user_name</td>
		    <td>该优惠券仅限在$bonusType.use_start_date.ToString("yyyy-MM-dd")至$bonusType.use_end_date.ToString("yyyy-MM-dd")使用，并且单笔订单金额不能低于￥$bonusType.min_goods_amount</td>
		    <td>￥$bonusType.type_money</td>
		    <td>
		        #if ($row.order_id > 0)
		        已使用(时间：$row.used_time)
		        #else
		        未使用
		        #end,
		        #if($row.is_enbled)
		        已激活
		        #else
		        未激活
		        #end
		    </td>
		</tr>
		#end
	</tbody>
</table>
<div class="page">
    $pager
</div>